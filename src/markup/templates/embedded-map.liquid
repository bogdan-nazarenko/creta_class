<iframe
    loading="lazy"
    class="{% if className %}{{ className }} {% endif %}embedded-map"
    src="{{ props.map }}"
    referrerpolicy="no-referrer-when-downgrade"
    allowfullscreen
></iframe>
